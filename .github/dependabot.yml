version: 2
updates:
  # Actions are pinned to commit SHAs for supply-chain safety, which means
  # nothing updates them without help. Dependabot opens the PRs.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
    commit-message:
      prefix: "ci"
    labels: ["dependencies"]
